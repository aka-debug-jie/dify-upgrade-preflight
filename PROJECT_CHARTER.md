# 项目目标与产品边界

状态：执行前设计基线；需要通过 P00 才进入业务实现。

## 1. 产品定义

暂用名 Dify Upgrade Preflight；分发包名候选 `dify-upgrade-preflight`、导入模块 `dify_preflight`，CLI 候选 `dify-preflight`。名称尚未做包仓库、商标和重名核查，不声称已占用。

目标用户是维护一套自托管 Dify、使用 Docker Compose、对本地 `.env` 或服务配置有修改、准备升级而需要复核的人。

核心承诺：**把固定的官方升级证据匹配到明确的部署事实，给出可追溯、范围清楚的升级分析。**它是检查器，不是部署器、运维平台代理或通用安全审计器。

## 2. 第一版的四项交付

1. 收集明确授权的本地配置，生成脱敏 `DeploymentSnapshot`；标明声明值及未观察的运行状态。
2. 比较当前官方基线 A、用户配置 B、目标官方基线 C，区分上游变化、本地定制和分歧；可选输入用户准备采用的目标配置 D。
3. 按经过审核的 source→target 支持边及规则，生成 findings、人工检查项与证据缺口。
4. 输出终端/JSON/Markdown；核心离线可运行，具备稳定退出码、固定测试夹具和性能基线。

首版目标是 **2 条明确升级边、至少 6 个独立根因家族的真实案例**。不存在证据时不凑数，不由两个相邻升级边推导一条未经测试的跨版本边。

## 3. 优先级

| 优先级 | 要求 |
|---|---|
| P0 | 不改生产、不泄露凭据、不把未知说成通过 |
| P1 | 一个真实端到端场景可用，依赖少，输入输出可验证 |
| P2 | 明确且可实测的时延、内存、输入上限和离线能力 |
| P3 | 规则覆盖、安装体验、兼容性 |
| P4 | 美观、额外格式和生态扩张 |

“性能”同时包括检测有效性和运行效率。禁止为了跑得快而跳过必要检查；也禁止为了规则数量牺牲准确性。

## 4. 分层支持，而不是一上来读生产数据库

v0.1：Linux 为发布目标；macOS 先支持开发及离线 JSON 分析，只有实测后写入支持清单。Python 3.11–3.13 是拟定测试矩阵，P00 核验可用环境，未跑的平台不得标为已验证。核心分析不要求 Docker daemon；从原始 Compose 采集时需要经验证的 Docker Compose CLI。

部署范围：Docker Compose；PostgreSQL 部署画像；优先 bundled Weaviate，外部向量库只能识别为外部并明确不检查其内部升级安全。其他数据库、Kubernetes、源码启动、开发分支、nightly、未经映射的自定义镜像均不在首版完整检查范围。

v0.2 候选：显式启用 Docker 运行事实读取，以及单独设计的数据库元数据探测。只有首版被真实用户复用后才进入，不能作为“让首版完整”而提前加入。

## 5. 明确不做

不自动升级/修复/回滚；不修改或合并生产文件；不证明备份可恢复；不执行迁移；不运行 LLM；不训练模型；不做网页后台、SaaS、插件市场、通用多平台升级框架；不以 star 数决定功能。

## 6. 差异化必须在演示里可见

我们的重点不是再做 `.env` 同步脚本，而是：同一条官方升级提醒，在内置服务、外部服务、新部署、信息不全四种画像下给出不同且可解释的结论；同时保留本地定制与目标配置之间的关系。

官方已经有环境变量同步工具的来源见 `sources/SOURCES.md` 中 S01。该事实仅用于避免重复定位，不构成对当前所有竞品的完整排除结论。

## 7. 产品验证和止损

P00 若无法找到 2 类能可靠检测、超过普通 env diff 的真实问题，保留研究记录，不建设完整平台。
P05 若只能输出泛化提醒或绝大多数画像永远 INCOMPLETE，收缩支持范围并复审产品价值。
P08/P09 以至少 3 个不同部署画像、其中至少 2 个独立维护者的脱敏反馈验证可用性；拿不到真实反馈就保持 beta，不自称生产级。
任何错误放行、凭据泄露或未经授权写入均冻结发布。修复后必须增加反例再恢复支持。

## 8. 权限与成功定义

你是产品、规则和发布审批人；Codex 是实现者和证据整理者，不是事实的最终权威。
成功不是“生成很多文件/测试/星标”，而是：用户能安装、输入合法、报告有用、来源可查、未知明确、核心性能达标、维护一条新升级边不需要改造整个架构。
