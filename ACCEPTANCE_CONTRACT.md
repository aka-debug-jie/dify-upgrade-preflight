# 全局验收与完成定义

版本 0.1.0。下列数字是**工程验收目标，不是已经测得的结果**。变更必须先提交变更申请，不能通过放宽测试让实现过关。

## A. 首版发布范围

v0.1 的公开候选版需要 P00–P08 通过，并由用户授权发布。稳定版本还需要 P09 的真实使用证据。P10/P11 不阻塞首版，不得提前扩展。

支持矩阵必须包含明确 source/target release、固定源码 commit、部署画像、采集器版本、检查范围和案例。初始批准边数为 0；首版目标为 2，不宣称覆盖一个连续大版本区间。

## B. 正确性硬闸门

- 自行裁决并锁定的对抗案例中，已知危险输入错误输出 NO_KNOWN_BLOCKERS 的数量必须为 0。只适用于该测试集，禁止推断线上错误率为 0。
- 关键未知输入正确输出 INCOMPLETE；范围外输入 UNSUPPORTED；操作失败 ERROR。四种情况不能混为“通过”。
- 每条生产规则必须有：官方固定证据、适用画像、版本边界、正例、反例、未知例、旧规则失效/覆盖测试、审批记录。
- 至少 6 个独立根因家族的真实升级案例，以及至少 30 个边界/反例/未知案例。复制不同版本号不算新根因。
- 三方配置差异不自动定为 blocker；没有业务证据的分歧必须显示人工合并建议。
- 声明版本、运行版本、历史升级路径不互相替代。
- 所有结果包含 scope、规则目录 digest、来源引用、未检查项、规则适用性/观察事实。
- 审核案例中的多余 blocker 为 0；其他 warning 的可操作率由人工标注报告，不用单一覆盖率掩盖噪声。

## C. 隐私与只读

全部测试（正常、异常、debug、JSON、Markdown）中的 canary secret 泄露次数为 0。
核心 check 的 socket/subprocess 调用次数为 0；部署输入内容 hash 在前后保持一致。只读验收还需捕获写调用，不能仅用 mtime 或 hash 判断安全。
路径逃逸、恶意 YAML、重复键、超大输入、未知 schema、错误 digest 都必须受控退出；不能抛出带原始秘密的栈。

## D. 性能目标

参考环境：Linux x86_64、2 vCPU、8 GiB RAM、本地 SSD、干净虚拟环境；记录实际 CPU/OS/Python/依赖和容器限额。其他硬件结果单列。

| 工作负载 | 目标 |
|---|---|
| 新进程 `--help`，已安装依赖、不联网 | p95 ≤ 0.5 秒 |
| 中型快照：60 服务、2,000 类型事实、250 条规则、JSON ≤ 2 MiB | check 新进程 p95 ≤ 2 秒，峰值 RSS ≤ 200 MiB |
| 压力快照：200 服务、10,000 类型事实、1,000 条规则、JSON ≤ 12 MiB | check 新进程 p95 ≤ 8 秒，峰值 RSS ≤ 384 MiB |
| Compose snapshot：允许清单内 ≤ 100 个文件，总输入 ≤ 32 MiB | p95 ≤ 10 秒；外部解析单次硬超时 15 秒，总采集硬超时 30 秒 |
| 不依赖 Docker/网络的开发测试 | 参考机 ≤ 60 秒；否则拆分快慢套件，核心门禁仍有记录 |

每组 5 次预热、30 次正式样本，保存全部时延而不只保存均值。运行时限目标未达不能删检测项；先 profile，再优化。共享 CI 的绝对时延只作监测，正式性能闸门使用参考配置或经审批的新参考环境。

## E. 工程交付

CLI 安装、`--help`、synthetic demo、snapshot、check、三种报告和退出码契约通过黑盒测试。
同一规范化输入和同一 catalog 产生相同 decision_digest；时间戳、耗时、主机定位不参与该 digest。
关键 evaluator、verdict、脱敏和路径保护分支全覆盖；对其余模块记录覆盖率，不以总行覆盖率替代语义案例。关键版本/优先级变异必须被测试杀死。
轮子包/sdist 在新虚拟环境中可安装运行，且不包含真实配置、原始证据大文件、未批准规则或实验凭据。

## F. 一阶段完成定义

该阶段有真实 baseline、具体改动、执行命令/返回码、测试材料、风险清单；所有本阶段验收通过后仅标记 `ready_for_review`。用户批准且审批对象未变化后才能由执行者转为 `done` 并解锁下一阶段。

## G. 不能声称的结论

没有真实升级重演：不能说已验证真实升级。
没有用户部署反馈：不能说生产级。
没有性能原始记录：不能说达到性能目标。
没有审批：不能说规则已认证、版本已支持。
只有启动包文件验证：不能说产品已开发完成。
