这次只审查我明确指定的新Dify release。若未给目标版本，从最新owner任务获取；不要自行把latest当已授权目标。

读取规则/证据/维护契约，固定release、源码commit与内容digest；比较当前已支持基线，列出候选变化：配置布局、组件镜像、前置要求、一次性迁移和旧规则是否被取代。

来源或版本行为冲突就记录；不运行下载的脚本，不导入migration代码，不执行升级。

逐项输出：明确事实、适用画像、需要的本地facts、before/during/after类别、正反未知fixture、旧路径回归影响、候选严重级别。

本轮只能生成candidate与审核卡，不自动新增approved edge、不自动退休旧规则、不改变规则严重级别、不发布。

没有高置信新需求时输出“无需增加规则”也是有效结果。不要为了版本更新而凑规则数量。
