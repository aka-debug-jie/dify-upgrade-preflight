# 变更申请格式

必须说明：当前契约/规则、拟修改内容、证据、为何现有设计不能满足、替代方案、用户价值、实现成本、性能影响、安全/隐私影响、受影响 fixture、迁移兼容性。

明确标记是否改变：验收条件、支持范围、核心依赖、规则严重级别、目录信任、默认网络/文件/进程权限、公开 CLI/schema、发布渠道。

保持现有基线不变直到 owner 明确批准；批准要绑定申请和修改对象 digest，随后再更新契约锁。不能先改 hash 再请求批准。
